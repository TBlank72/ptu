
extends layout

block content
  include ./includes/header.jade
  .container-fluid.bg-navy-gradient
    if (message.length > 0)
      .msg-container
        .alert.alert-danger #{message}
    .form-container
      h1 #[span(class='fa fa-lock')] Secure Signup
      form(action='/signup', method='post')
        .form-group
          input.form-control.bg-navy-gradient(type='text', name='name', placeholder='Full Name (for Certificate)')
        .form-group
          input.form-control.bg-navy-gradient(type='text', name='email', placeholder='Email')
        .form-group
          input.form-control.bg-navy-gradient(type='password', name='password', placeholder='Password')
        button.btn.btn-custom(type='submit') Signup
        br
        p Already have an account? #[a(href='/login') Login]
  include ./includes/footer.jade
